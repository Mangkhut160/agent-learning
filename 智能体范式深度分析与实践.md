# 智能体范式深度分析与实践

> 基于《Hello Agents》第四章——ReAct、Plan-and-Solve、Reflection 三种经典智能体范式

---

## 第一部分：三种范式的本质对比

### 1.1 "思考"与"行动"的组织方式——本质区别

三种范式的核心差异在于**决策时机**和**信息流向**两个维度：

```
决策时机轴:
  边做边想 ←——————————————————→ 先想再做
  (ReAct)        (Reflection)    (Plan-and-Solve)

信息流向轴:
  外部反馈驱动 ←————————————————→ 内部推理驱动
  (ReAct)        (Reflection)    (Plan-and-Solve)
```

**ReAct——交替式 (Interleaved)**

思考（Thought）和行动（Action）在同一循环内交替执行。每次思考只规划**下一步**，而非全局。行动的结果（Observation）立刻被注入下一轮思考。这种组织方式的核心特征是**行动的结果会改变后续思考的方向**。

形式化表达：

```
Thought₁ → Action₁ → Observation₁ → Thought₂ → Action₂ → ... → Answer
```

**Plan-and-Solve——分离式 (Decoupled)**

规划和执行被彻底分离为两个独立阶段。规划阶段产出**完整的静态计划**，执行阶段**严格按照计划**逐步推进。执行过程中不会修改计划本身。

形式化表达：

```
Question → Plan = [Step₁, Step₂, ..., Stepₙ]
         → Execute(Step₁) → Execute(Step₂) → ... → Execute(Stepₙ) → Answer
```

**Reflection——迭代式 (Iterative)**

执行和反思交替，但**反思的结果作用于当前方案的整体改进**，而非下一步的局部决策。每次迭代产生一个**完整的新版本**。

形式化表达：

```
Execute₀ → Reflect₁ → Execute₁ → Reflect₂ → Execute₂ → ... → 最优解
```

**关键区别总结：**


| 维度   | ReAct    | Plan-and-Solve | Reflection |
| ---- | -------- | -------------- | ---------- |
| 思考粒度 | 下一步      | 整个计划           | 整个方案       |
| 行动粒度 | 单个工具调用   | 单个子步骤          | 完整任务       |
| 信息增量 | 外部观察     | 前序步骤结果         | 批判性反馈      |
| 灵活性  | 高（可随时调整） | 低（计划固定）        | 中（迭代改进）    |
| 收敛方式 | 达到最终答案   | 执行完所有步骤        | 评审满意或达到上限  |
| 适用场景 | 信息获取型任务  | 结构化推理任务        | 质量优化型任务    |


---

### 1.2 智能家居控制助手——范式选择分析

**场景分析：**

智能家居控制助手具有以下特征：

- 需要与多个物理设备交互（灯光、空调、窗帘等）——涉及工具调用
- 设备状态会因用户指令而实时变化——需要获取外部反馈
- 用户习惯是长期模式，需要从历史中学习——需要记忆
- 场景间有联动关系（如"睡眠模式"需要同时关闭灯光、调节空调、拉上窗帘）

**推荐：以 ReAct 为基础架构，融合 Reflection 的混合方案。**

理由如下：

1. **设备控制天然需要 ReAct 的"观察"能力**：打开空调后，需要检查空调是否成功响应（Observation），这个反馈必须影响后续决策。Plan-and-Solve 的静态计划无法应对"空调启动失败"这样的异常。
2. **多设备联动需要逐步确认**：执行"睡眠模式"时，每个设备的操作结果都可能影响后续步骤（窗帘卡住了→可能需要调整灯光亮度来补偿）。ReAct 的 Thought-Action-Observation 循环天然适合。
3. **习惯学习需要 Reflection 的迭代优化**：用户的习惯不是一次就能学到的。系统可以每天反思"昨天的控制策略是否匹配用户的真实行为"，然后优化策略。

具体架构设计：

```
┌─────────────────────────────────────────────────┐
│              智能家居控制助手                      │
├─────────────────────────────────────────────────┤
│  每日离线层 (Reflection)                          │
│  ├─ 分析昨日用户行为与控制策略的偏差                │
│  ├─ 反思策略质量，生成改进建议                      │
│  └─ 更新"习惯规则库"                              │
├─────────────────────────────────────────────────┤
│  实时控制层 (ReAct)                               │
│  ├─ Thought: 分析当前意图 + 查询习惯规则库          │
│  ├─ Action: 调用设备控制工具                       │
│  ├─ Observation: 获取设备状态反馈                   │
│  └─ 循环直到场景执行完毕                           │
└─────────────────────────────────────────────────┘
```

---

### 1.3 三种范式的组合使用

**可以组合，且组合后的效果远超单一范式。**

#### 混合架构设计：P+R³ 框架

```
┌──────────────────────────────────────────────────────────────┐
│                    P+R³ 混合智能体架构                         │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌─────────┐    ┌──────────┐    ┌─────────┐    ┌───────────┐ │
│  │ Planner │ →  │ Executor │ →  │Reflector│ →  │ Refiner   │ │
│  │ (Plan)  │    │ (ReAct)  │    │(Reflect) │    │ (Reflect) │ │
│  └─────────┘    └──────────┘    └─────────┘    └───────────┘ │
│       │              │               │               │       │
│       ▼              ▼               ▼               ▼       │
│  静态计划生成   每步用ReAct执行   评估执行结果    优化整个方案  │
│                                                              │
│  适用场景: 需要工具调用、步骤间有依赖、质量要求高的复杂任务     │
└──────────────────────────────────────────────────────────────┘
```

**工作流程：**

1. **Plan（规划层）**：将用户意图分解为高层次子任务
2. **Execute（执行层）**：每个子任务用 ReAct 方式执行（可调用工具、获取反馈）
3. **Reflect（反思层）**：评估执行结果的整体质量
4. **Refine（优化层）**：如果质量不达标，优化方案并重新执行

**适用场景举例：**


| 场景      | 使用的范式组合                   | 原因                                 |
| ------- | ------------------------- | ---------------------------------- |
| 复杂旅行预订  | Plan + ReAct              | 先规划行程框架，每步预订用ReAct处理实时价格/库存        |
| 代码生成与部署 | Plan + Reflection         | 先规划架构，再迭代优化代码质量                    |
| 客服系统    | ReAct + Reflection        | 实时查询信息(ReAct)，争议问题自我反思(Reflection) |
| 研究实验设计  | Plan + ReAct + Reflection | 规划实验方案 → 执行实验并观察 → 反思结果并迭代         |


---

## 第二部分：ReAct 输出解析的鲁棒性

### 2.1 当前正则解析的脆弱性分析

当前 `_parse_output` 方法存在以下脆弱点：

```python
# 脆弱点1: 依赖特定的格式标记
thought_match = re.search(r"Thought:\s*(.*?)(?=\nAction:|$)", text, re.DOTALL)

# 脆弱点2: Action格式假设过于简单
match = re.match(r"(\w+)\[(.*)\]", action_text, re.DOTALL)
```

**失败场景清单：**


| 场景                | 示例                      | 失败原因                                     |
| ----------------- | ----------------------- | ---------------------------------------- |
| LLM 使用中文冒号        | `思考：xxx`                | `Thought:` 不匹配                           |
| 多行 Action 参数      | `Search[华为\n最新手机]`      | `.`* 匹配换行时贪婪/非贪婪边界模糊                     |
| Action 参数含 `]`    | `Search[华为P70 [Pro版]]`  | 嵌套括号导致提前截断                               |
| LLM 输出额外文字        | `我的思考是... Thought: xxx` | 前缀干扰匹配                                   |
| LLM 省略标记          | `搜索华为手机`                | 完全无法解析                                   |
| 模型返回 `<think>` 标签 | MiniMax 的推理过程           | 污染解析内容                                   |
| 工具名含数字            | `GPT4_Search[query]`    | `\w+` 可匹配，但 LLM 可能输出 `GPT-4_Search` 导致截断 |


### 2.2 更鲁棒的解析方案

**方案 A：JSON 结构化输出（推荐）**

```python
# 要求 LLM 输出 JSON 而非自由文本
REACT_JSON_PROMPT_TEMPLATE = """
请以 JSON 格式输出你的思考与行动，严格遵守以下 schema：

{
    "thought": "你的思考过程",
    "action": {
        "tool": "工具名称",
        "input": "工具输入参数"
    }
}

如果已有最终答案，使用：
{
    "thought": "思考过程",
    "final_answer": "最终答案"
}

当前问题: {question}
历史记录: {history}
可用工具: {tools}

请只输出 JSON，不要包含其他文字：
"""

# 解析代码
import json

def _parse_output_json(self, text: str) -> dict:
    # 尝试提取 JSON 块
    json_match = re.search(r'\{[\s\S]*\}', text)
    if json_match:
        try:
            return json.loads(json_match.group())
        except json.JSONDecodeError:
            pass
    return None
```

**方案 B：Function Calling / Tool Use（最鲁棒）**

利用 OpenAI 兼容 API 的原生 function calling 能力，让模型直接输出结构化的 tool call：

```python
def think_with_tools(self, messages, tools_schema):
    response = self.client.chat.completions.create(
        model=self.model_name,
        messages=messages,
        tools=tools_schema,
        tool_choice="auto"
    )
    msg = response.choices[0].message
    if msg.tool_calls:
        return {
            "type": "tool_call",
            "tool_name": msg.tool_calls[0].function.name,
            "arguments": json.loads(msg.tool_calls[0].function.arguments)
        }
    else:
        return {
            "type": "text",
            "content": msg.content
        }
```

**方案 C：XML/标签式输出**

```xml
<thought>需要搜索华为最新手机</thought>
<action type="Search">华为最新手机</action>
```

优缺点对比：


| 方案               | 鲁棒性 | 实现复杂度 | LLM兼容性         | 适用场景        |
| ---------------- | --- | ----- | -------------- | ----------- |
| 正则(当前)           | 低   | 低     | 所有模型           | 原型验证        |
| JSON             | 中高  | 中     | 大部分模型(偶有格式错误)  | 生产环境        |
| Function Calling | 最高  | 中     | 仅支持tool use的模型 | 生产环境首选      |
| XML标签            | 中   | 低     | 所有模型           | 介于正则和JSON之间 |


---

## 第三部分：工具系统扩展实践

### 3.1 计算器工具实现

```python
import math
import operator
import re
from typing import Union

def calculator(expression: str) -> str:
    """
    安全地计算数学表达式。

    支持的运算: 加减乘除、幂运算、括号、三角函数、sqrt、log
    示例: "(123 + 456) * 789 / 12", "sqrt(144)", "sin(pi/2)"

    Args:
        expression: 数学表达式字符串

    Returns:
        计算结果的字符串
    """
    print(f"🧮 正在计算: {expression}")

    # 白名单安全沙箱
    safe_dict = {
        "abs": abs, "round": round, "min": min, "max": max,
        "pow": pow, "sqrt": math.sqrt,
        "sin": math.sin, "cos": math.cos, "tan": math.tan,
        "log": math.log, "log10": math.log10, "log2": math.log2,
        "pi": math.pi, "e": math.e,
        "ceil": math.ceil, "floor": math.floor,
    }

    # 移除危险字符，只保留安全字符
    if not re.match(r'^[\d\s+\-*/().,%a-zA-Z_]+$', expression):
        return "错误: 表达式包含不允许的字符"

    try:
        result = eval(expression, {"__builtins__": {}}, safe_dict)
        return f"计算结果: {result}"
    except Exception as e:
        return f"计算错误: {e}"
```

### 3.2 工具选择失败处理机制

```python
class ToolExecutorWithRecovery(ToolExecutor):
    """增强版工具执行器，具备故障恢复能力"""

    def __init__(self, max_retries: int = 3):
        super().__init__()
        self.max_retries = max_retries
        self.consecutive_failures: dict[str, int] = {}  # 工具名 -> 连续失败次数

    def execute_with_recovery(self, tool_name: str, tool_input: str) -> str:
        """
        执行工具并处理失败情况。
        """
        # 1. 尝试执行
        result = self._try_execute(tool_name, tool_input)

        # 2. 如果失败，尝试纠错
        if result.startswith("错误"):
            corrected = self._attempt_correction(tool_name, tool_input, result)
            if corrected:
                return corrected

        return result

    def _try_execute(self, tool_name: str, tool_input: str) -> str:
        tool_func = self.getTool(tool_name)
        if not tool_func:
            self._record_failure(tool_name)
            similar = self._suggest_similar_tools(tool_name)
            return f"错误: 未找到工具 '{tool_name}'。" + (f"\n你可能想使用: {similar}" if similar else "")

        try:
            result = tool_func(tool_input)
            self._reset_failure(tool_name)
            return result
        except Exception as e:
            self._record_failure(tool_name)
            return f"错误: 工具执行失败 - {e}"

    def _attempt_correction(self, tool_name: str, tool_input: str, error: str) -> str | None:
        """尝试自动纠正参数格式等问题"""
        # 计算器参数纠正常见问题
        if tool_name == "Calculator":
            # 尝试修复括号不匹配
            if tool_input.count("(") != tool_input.count(")"):
                fixed = tool_input.replace("（", "(").replace("）", ")")
                if fixed != tool_input:
                    print(f"🔧 自动纠正括号: {tool_input} → {fixed}")
                    return self._try_execute(tool_name, fixed)

        return None

    def _suggest_similar_tools(self, name: str) -> str:
        """基于编辑距离建议相似工具"""
        from difflib import get_close_matches
        matches = get_close_matches(name, self.listTools(), n=3, cutoff=0.4)
        return ", ".join(matches) if matches else ""

    def _record_failure(self, tool_name: str):
        self.consecutive_failures[tool_name] = \
            self.consecutive_failures.get(tool_name, 0) + 1

    def _reset_failure(self, tool_name: str):
        self.consecutive_failures[tool_name] = 0

    def should_intervene(self) -> str | None:
        """
        检查是否需要系统介入。当某工具连续失败超过阈值时，
        返回一个引导性提示，帮助 LLM 调整策略。
        """
        for tool_name, count in self.consecutive_failures.items():
            if count >= self.max_retries:
                return (
                    f"⚠️ 工具 '{tool_name}' 已连续失败 {count} 次。"
                    f"建议: 1) 检查输入参数格式是否正确 "
                    f"2) 尝试使用替代工具 "
                    f"3) 可用工具列表: {self.listTools()}"
                )
        return None
```

在 `ReActAgent.run()` 的循环中插入：

```python
# 在主循环中加入故障恢复检查
intervention = self.tool_executor.should_intervene()
if intervention:
    if self.verbose:
        print(f"🆘 系统介入: {intervention}")
    self.history.append(f"System: {intervention}")
```

### 3.3 大规模工具集的管理策略（50-100+ 工具）

当工具数量达到 50-100 个时，直接注入全部描述到提示词会导致：

- **上下文窗口溢出**：100 个工具×100 字描述 = 10K tokens，挤压推理空间
- **注意力稀释**：LLM 对前面的工具描述关注度下降
- **选择困难**：工具间语义重叠，LLM 难以精确选择

**优化方案：**

```
┌─────────────────────────────────────────────────┐
│            分层工具管理架构                       │
├─────────────────────────────────────────────────┤
│  Layer 1: 工具注册中心                           │
│  ├─ 工具元数据: name, description, schema, tags  │
│  ├─ 语义向量索引 (embedding-based retrieval)      │
│  └─ 工具分组: 按领域分类(数学/搜索/设备/...)      │
├─────────────────────────────────────────────────┤
│  Layer 2: 动态工具检索                           │
│  ├─ 意图→工具映射: LLM先输出意图，再检索相关工具    │
│  ├─ 向量相似度检索: 用embedding匹配工具描述        │
│  └─ 层级过滤: 先大类后小类，逐步缩小候选集         │
├─────────────────────────────────────────────────┤
│  Layer 3: 工具选择决策                           │
│  ├─ 候选工具集注入提示词(通常 3-5 个)             │
│  ├─ LLM 从候选中选择最合适的                      │
│  └─ 选择失败回退到更大候选集                      │
└─────────────────────────────────────────────────┘
```

**伪代码实现：**

```python
class ScalableToolRegistry:
    def __init__(self):
        self.tools: dict[str, ToolMeta] = {}
        self.category_index: dict[str, list[str]] = {}
        self.embedding_index = None  # 向量索引（可选用 faiss/chroma）

    def retrieve(self, intent: str, top_k: int = 5) -> list[str]:
        """根据用户意图，检索最相关的工具"""
        # 方案1: 基于分类层级
        category = self._classify_intent(intent)  # 如 "math", "search", "device"
        candidates = self.category_index.get(category, [])

        # 方案2: 基于语义向量（当分类不明确时）
        if not candidates or len(candidates) > 20:
            candidates = self._semantic_search(intent, top_k)

        return candidates[:top_k]

    def _classify_intent(self, intent: str) -> str:
        """用轻量级模型或关键词判断意图类别"""
        # 可以用一个快速的 LLM 调用或规则匹配
        ...
```

**方案对比：**


| 方案         | 延迟  | 精度     | 成本        | 适用规模      |
| ---------- | --- | ------ | --------- | --------- |
| 全文注入       | 低   | 中(小规模) | 高(tokens) | < 20 工具   |
| 分类层级       | 低   | 中      | 低         | 20-100 工具 |
| 向量检索       | 中   | 高      | 低         | 100+ 工具   |
| 两阶段(粗筛+精选) | 中   | 最高     | 中         | 任意规模      |


---

## 第四部分：Plan-and-Solve 的深度优化

### 4.1 动态重规划机制设计

当前实现中计划是静态的。执行过程中如果某步失败，整个流程就中断了。以下是动态重规划机制的设计：

```
┌──────────────────────────────────────────────────────┐
│              动态重规划 (Dynamic Replanning)            │
├──────────────────────────────────────────────────────┤
│                                                      │
│  Plan = [Step₁, Step₂, Step₃, Step₄, Step₅]          │
│              │                                       │
│              ▼                                       │
│         Execute Step₁ → ✅ 成功                       │
│              │                                       │
│              ▼                                       │
│         Execute Step₂ → ❌ 失败 (结果不符合预期)       │
│              │                                       │
│              ▼                                       │
│    ┌─────────────────────────┐                       │
│    │    重规划决策模块         │                       │
│    │  输入: 失败步骤、历史结果  │                       │
│    │  输出: 调整后的计划       │                       │
│    └────────┬────────────────┘                       │
│             │                                       │
│             ▼                                       │
│     新 Plan = [Step₁_done, Step₂', Step₃', ..., Stepₙ']│
│             │                                       │
│             ▼                                       │
│      从 Step₂' 继续执行                               │
│                                                      │
└──────────────────────────────────────────────────────┘
```

**代码设计：**

```python
class DynamicPlanSolveAgent(PlanAndSolveAgent):
    def __init__(self, llm_client, max_replans: int = 3):
        super().__init__(llm_client)
        self.max_replans = max_replans

    def run(self, question: str) -> str:
        plan = self.planner.plan(question)
        if not plan:
            return None

        completed_steps = []  # [(step, result), ...]
        pending_steps = plan[:]

        replan_count = 0

        while pending_steps and replan_count <= self.max_replans:
            step = pending_steps.pop(0)
            result = self._execute_single_step(question, plan, completed_steps, step)

            if self._is_result_acceptable(result):
                completed_steps.append((step, result))
            else:
                print(f"⚠️ 步骤 '{step}' 结果不符合预期，触发重规划...")
                replan_count += 1

                # 重规划: 基于已完成步骤 + 失败步骤，重新生成剩余计划
                new_tail = self._replan(
                    question=question,
                    completed=completed_steps,
                    failed_step=step,
                    failed_result=result,
                    remaining=pending_steps
                )pending_steps = new_tail

        return completed_steps[-1][1] if completed_steps else None

    def _is_result_acceptable(self, result: str) -> bool:
        """判断执行结果是否可接受"""
                
        # 简单的启发式判断
        error_signals = ["错误", "失败", "无法完成", "Error", "None", "[]"]
        return not any(s in str(result) for s in error_signals)

    def _replan(self, question, completed, failed_step, failed_result, remaining) -> list[str]:
        """生成调整后的新计划"""
        replan_prompt = f"""
原始问题: {question}
已成功完成的步骤:
{self._format_completed(completed)}

失败的步骤: {failed_step}
失败原因: {failed_result}

原本计划中的剩余步骤: {remaining}

请基于以上信息，生成调整后的新步骤列表（仅包含尚未完成的步骤）。
输出格式与规划阶段相同：```python ["新步骤1", "新步骤2", ...] ```
"""
        return self.planner.plan(replan_prompt)
```

### 4.2 旅行预订场景：Plan-and-Solve vs ReAct

**对于"预订北京→上海商务旅行（机票+酒店+租车）"，ReAct 更合适。**

分析：


| 维度    | ReAct               | Plan-and-Solve      |
| ----- | ------------------- | ------------------- |
| 机票预订  | ✅ 搜索时发现无票可立即调整日期/航司 | ❌ 计划写死"预订机票"，无法应对无票 |
| 酒店选择  | ✅ 根据机票时间动态调整酒店位置    | ❌ 计划预设的酒店区域可能不合理    |
| 价格联动  | ✅ 机票涨价后可降级酒店来平衡预算   | ❌ 静态计划无法感知价格波动      |
| 整体一致性 | ⚠️ 可能缺乏全局最优         | ✅ 三者在计划层面协调         |


该场景的核心特征是**高度不确定性**：价格实时波动、库存不可预知、各环节相互耦合。Plan-and-Solve 的静态计划会在第一个不可预见的障碍处卡死，而 ReAct 天然具备"观察→调整"的能力。

**最优解**是 Plan+ReAct：

- 先用 Plan 制定高层框架（预算分配、时间窗口、偏好排序）
- 每个预订步骤用 ReAct 执行（搜索 → 比较 → 失败时调整 → 确认）

### 4.3 分层规划系统设计

```
                    高层抽象计划
          ┌────────────────────────────┐
          │ 1. 确定旅行时间与预算        │
          │ 2. 完成交通安排              │
          │ 3. 完成住宿安排              │──→ 这些是人类可以审阅的粒度
          │ 4. 完成地面交通              │
          │ 5. 生成行程确认单            │
          └────────────────────────────┘
                     │
          ┌──────────┼──────────┐
          ▼          ▼          ▼
    子计划(交通)   子计划(住宿)  子计划(地面)
    ┌────────┐  ┌────────┐  ┌────────┐
    │2.1 搜索 │  │3.1 搜索 │  │4.1 搜索 │
    │2.2 比价 │  │3.2 对比 │  │4.2 预约 │
    │2.3 预订 │  │3.3 预订 │  │4.3 确认 │
    │2.4 确认 │  │3.4 确认 │  └────────┘
    └────────┘  └────────┘
```

**优势：**

1. **可控的抽象层级**：人类可以在高层计划阶段介入审批，而非盯着几十个微观步骤
2. **并行执行**：交通、住宿、地面交通三个子计划互不依赖，可以并行执行
3. **局部重规划**：如果机票预订失败，只需重新规划"交通"子计划，不影响住宿部分
4. **上下文管理**：每个子计划的上下文窗口只需要该领域的信息，不会装下全量历史
5. **调试可定位**：问题出在哪个子计划一目了然，便于排查

---

## 第五部分：Reflection 机制的深度分析

### 5.1 异构模型策略的影响

**场景：强大模型做反思 + 快速模型做执行**


| 维度       | 影响                                                     |
| -------- | ------------------------------------------------------ |
| **成本**   | 大幅降低。反思调用占比小（每轮 1 次），执行调用占比大（初版+每轮优化）。快速模型通常便宜 10-50 倍 |
| **延迟**   | 显著降低。快速模型推理速度通常快 2-5 倍                                 |
| **反思质量** | 可能更高。强大模型能发现更隐晦的问题（如边界条件、设计模式）                         |
| **执行质量** | 快速模型生成的代码可能不如强大模型，但反思反馈足够详细时可以弥补                       |
| **潜在风险** | 反思建议可能超出执行模型的理解能力，导致优化无法落地                             |


**最佳实践：**

```
Reflection Loop:
  快速模型(Coder)   → 生成代码
  强大模型(Reviewer) → 审查代码，给出反馈
  快速模型(Coder)   → 根据反馈优化代码
  强大模型(Reviewer) → 验证优化是否满足要求
```

这种模式类似于人类团队的"高级工程师 Code Review + 初级工程师 Coding"。

### 5.2 更智能的终止条件

当前"反馈包含'无需改进'或达到最大迭代"过于简单。更智能的方案：

```python
class SmartTermination:
    def __init__(self, llm_client):
        self.llm_client = llm_client
        self.improvement_history = []  # 记录每轮的改进幅度

    def should_terminate(self, current_code: str, previous_code: str,
                         feedback: str, iteration: int) -> tuple[bool, str]:
        """
        基于多维度判断是否应该终止。

        Returns:
            (should_stop, reason)
        """

        # 1. 显式信号: 评审明确表示无需改进
        if "无需改进" in feedback:
            return True, "评审专家确认已达最优"

        # 2. 改进停滞检测: 连续N轮代码变化小于阈值
        similarity = self._compute_similarity(current_code, previous_code)
        self.improvement_history.append(similarity)

        if len(self.improvement_history) >= 2:
            recent = self.improvement_history[-2:]
            if all(s > 0.95 for s in recent):
                return True, "改进幅度已收敛（连续两轮代码相似度 > 95%）"

        # 3. 质量目标检测: 代码是否满足预设的质量指标
        quality_score = self._evaluate_quality(current_code)
        if quality_score >= 0.9:
            return True, f"代码质量评分 {quality_score:.0%}，已达标"

        # 4. 退化检测: 新代码是否比旧代码差
        if iteration > 1 and self._is_worse(current_code, previous_code):
            return True, "当前修改导致质量退化，建议回退到上一版本"

        # 5. 成本控制: 防止无限循环
        if iteration >= self.max_iterations:
            return True, f"达到最大迭代次数 ({self.max_iterations})"

        return False, "继续优化"

    def _compute_similarity(self, code_a: str, code_b: str) -> float:
        """计算两段代码的相似度（可用 difflib.SequenceMatcher）"""
        import difflib
        return difflib.SequenceMatcher(None, code_a, code_b).ratio()

    def _evaluate_quality(self, code: str) -> float:
        """让 LLM 对代码质量打分"""
        prompt = f"""
        请对以下代码从0到1进行质量评分，考虑因素: 正确性、效率、可读性、健壮性。
        只输出一个浮点数，如 0.85。

        代码:
        ```python
        {code}
        ```
        """
        response = self.llm_client.think(
            [{"role": "user", "content": prompt}]
        )
        try:
            return float(response.strip())
        except ValueError:
            return 0.5

    def _is_worse(self, new_code: str, old_code: str) -> bool:
        """判断新代码是否退步"""
        new_score = self._evaluate_quality(new_code)
        old_score = self._evaluate_quality(old_code)
        return new_score < old_score - 0.05  # 5%容差
```

### 5.3 学术论文写作助手——多维度 Reflection 设计

```
┌─────────────────────────────────────────────────────────────────┐
│              学术论文写作助手 - 多维度 Reflection                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐    │
│  │ 逻辑评审  │   │ 方法评审  │   │ 语言评审  │   │ 引用评审  │    │
│  │ Reviewer  │   │ Reviewer  │   │ Reviewer  │   │ Reviewer  │    │
│  └─────┬─────┘   └─────┬─────┘   └─────┬─────┘   └─────┬─────┘    │
│        │               │               │               │         │
│        ▼               ▼               ▼               ▼         │
│  段落逻辑性评分    方法创新性评分    语言表达评分    引用规范评分    │
│  反馈:            反馈:            反馈:            反馈:         │
│  - 论点-论据链    - 与SOTA对比     - 语法/用词     - 引用格式     │
│  - 段落过渡      - 实验设计        - 学术风格      - 文献覆盖     │
│  - 逻辑漏洞      - 可复现性        - 冗余表达      - 时效性       │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │              评分聚合 + 优先级排序                        │   │
│  │  加权总分 = 0.3×逻辑 + 0.25×方法 + 0.25×语言 + 0.2×引用  │   │
│  │  优化顺序: 优先级从低分维度开始                           │   │
│  └─────────────────────────────────────────────────────────┘   │
│                             │                                   │
│                             ▼                                   │
│                    ┌─────────────────┐                          │
│                    │   综合优化器     │                          │
│                    │  基于所有反馈    │                          │
│                    │  生成新版本论文  │                          │
│                    └─────────────────┘                          │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**提示词设计示例（逻辑评审维度）：**

```python
LOGIC_REVIEW_PROMPT = """
你是一位顶级学术期刊的逻辑编辑（如 Nature/Science 级别）。
请对以下论文段落的**论证逻辑**进行严格审查。

# 审查维度:
1. 论点与论据的对齐性: 每个论点是否有充分的证据支撑？
2. 论证链条的完整性: 是否存在跳跃或隐含假设？
3. 逻辑一致性: 前后结论是否自洽？
4. 结构合理性: 段落组织是否有助于读者理解论证过程？

# 待审查文本:
{text}

# 请按以下格式输出:
## 总体评分 (1-10):
## 具体问题:
- 问题1: (引用原文) → 分析 → 改进建议
- 问题2: ...
## 优化后的版本:
(直接给出修改后的文字)
"""
```

---

## 第六部分：提示词工程深度分析

### 6.1 ReAct vs Plan-and-Solve 提示词结构对比

**ReAct 提示词结构（4.2节）：**

```
角色: "你是一个智能助手"（通用角色）
信息: 可用工具列表
约束: 必须按 "Thought: → Action:" 格式输出
示例: 少量格式示例
动态信息: {question}, {history}
```

结构特征：**线性、增量式**。提示词被设计为支持"每一步只决定下一步"的范式。history 的存在说明这是一个**有状态的对话**，提示词每轮都在变化。

**Plan-and-Solve 规划阶段提示词（4.3节）：**

```
角色: "顶级AI规划专家"（专业角色）
任务: "将复杂问题分解成多个简单步骤"
约束: 输出 Python 列表，用 ```python 包裹
格式范例: ["步骤1", "步骤2", "步骤3"]
动态信息: {question}（仅此一个变量）
```

结构特征：**静态、全局式**。提示词只调用一次，输出的计划是整个任务的全貌。没有 history 变量——因为规划阶段不需要知道执行的中间结果。

**差异如何服务各自范式的核心逻辑：**


| 维度   | ReAct 提示词            | Plan-and-Solve 提示词 |
| ---- | -------------------- | ------------------ |
| 输出格式 | 自由文本(Thought/Action) | 结构化列表(Python list) |
| 为什么  | 行动类型多样，格式灵活          | 计划需被程序解析和遍历        |
| 状态管理 | history 变量，不断变化      | 无 history，一次性输出    |
| 为什么  | 每一步依赖上一步的观察          | 规划时不需要执行结果         |
| 角色设定 | 通用助手                 | 顶级规划专家             |
| 为什么  | 需要多角色(思考+行动)         | 只需专注分解任务           |
| 示例   | 格式示例(Thought/Action) | 列表格式示例             |
| 为什么  | 防止格式漂移               | 确保可解析              |


**核心洞察**：提示词结构 = 范式逻辑的外在表现。ReAct 提示词的设计目标是在不确定性中保持方向感；Plan-and-Solve 提示词的设计目标是一次性产出可执行的完整蓝图。

### 6.2 角色设定对智能体行为的影响

**实验：修改 Reflection 评审员角色**


| 角色设定                   | 评审重点            | 典型输出差异                                  |
| ---------------------- | --------------- | --------------------------------------- |
| "极其严格的代码评审专家，对性能有极致要求" | 算法复杂度、执行效率      | "O(n²)不可接受，必须优化到O(n log n)"             |
| "注重代码可读性的开源项目维护者"      | 变量命名、注释、结构清晰度   | "变量名 `x` 不够描述性，建议改为 `prime_candidates`" |
| "安全审计专家"               | SQL注入、输入验证、权限检查 | "缺少输入边界检查，n为负数时会导致未定义行为"                |
| "教学导向的编程导师"            | 概念解释、学习曲线、代码注释  | "建议添加详细注释解释筛法原理，便于初学者理解"                |


**影响规律：**

1. **角色设定决定了评审的"注意力焦点"**：同一个工具方法，不同角色看到的问题完全不同
2. **角色权威性影响批判力度**："专家"角色比"助手"角色更敢于指出严重问题
3. **角色+具体场景约束 > 泛化角色**：单纯"你是一个专家"效果远不如"你是Nature级别的逻辑编辑"

### 6.3 Few-shot 示例的效果对比

为 ReAct 的解析稳定性添加 few-shot 示例：

```python
REACT_PROMPT_WITH_FEWSHOT = """
你是一个智能助手。请严格按照以下格式输出，否则无法被系统识别。

## 可用工具
{tools}

## 输出格式（必须严格遵守）
Thought: 你对问题的分析
Action: 你要执行的行动

## 示例1:
Question: 华为最新手机是什么？
Thought: 我无法从训练数据中获取最新信息，需要使用搜索工具。
Action: Search[华为最新手机型号 2025]

## 示例2:
Question: 计算 15 * 30 + 25
Thought: 这是一个数学计算问题，我需要使用计算器工具。
Action: Calculator[15 * 30 + 25]

## 示例3:
Question: 今天天气怎么样？
Thought: 天气信息是实时数据，我需要搜索。
Action: Search[今天天气]

现在开始：
Question: {question}
History: {history}
"""
```

**效果对比（理论预期）：**


| 指标       | 无 Few-shot | 有 Few-shot           |
| -------- | ---------- | -------------------- |
| 格式遵循率    | 70-85%     | 90-98%               |
| 工具选择准确率  | 取决于模型      | 显著提升(尤其小模型)          |
| 输出长度控制   | 容易发散       | 更精炼                  |
| Token 消耗 | 低          | 中等(3个示例约+500 tokens) |
| 对新工具的泛化  | 较好         | 可能过拟合示例格式            |


---

## 第七部分：电商客服智能体——综合实战设计

### 7.1 核心架构选择

**选择：ReAct + Reflection 混合架构**

```
┌─────────────────────────────────────────────────────────────────┐
│                    客服智能体系统架构                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  用户退款请求                                                    │
│       │                                                         │
│       ▼                                                         │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │              ReAct 主循环 (信息收集与决策)                  │   │
│  │                                                         │   │
│  │  Thought: 分析退款原因 → 需要查询哪些信息？                │   │
│  │  Action: LookupOrder[order_id] → Observation: 订单详情   │   │
│  │  Action: CheckLogistics[order_id] → Observation: 已签收  │   │
│  │  Action: CheckPolicy[退款原因] → Observation: 符合条件   │   │
│  │  Thought: 判断是否批准                                    │   │
│  │                                                         │   │
│  │  判断置信度 > 阈值？                                       │   │
│  │    ├─ 是 → Action: SendEmail[批准退款邮件]                │   │
│  │    └─ 否 → 进入 Reflection 子循环                         │   │
│  └─────────────────────────────────────────────────────────┘   │
│       │ (低置信度)                                              │
│       ▼                                                         │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │           Reflection 子循环 (争议处理)                     │   │
│  │                                                         │   │
│  │  Reflect: 反思为什么不确信？缺少哪些信息？                  │   │
│  │          "用户声称未收到但物流显示已签收，需要核实签收人"    │   │
│  │  Action: 补充查询 → LookupDeliveryDetail[order_id]        │   │
│  │  Reflect: "签收人与收货人姓名一致，但需确认是否为本人"      │   │
│  │  Decision: 建议转人工复核 + 生成初步建议                    │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 7.2 所需工具清单

```python
# 工具1: 订单查询
ORDER_LOOKUP_TOOL = {
    "name": "LookupOrder",
    "description": (
        "查询用户的订单详细信息，包括商品、金额、下单时间、支付状态。"
        "输入: 订单号(order_id) 或 用户ID(user_id)+时间范围"
    ),
    "input_schema": {
        "order_id": "string (optional)",
        "user_id": "string (optional)",
        "date_range": "string (optional) - 如 '2025-01-01:2025-06-01'"
    }
}

# 工具2: 物流追踪
LOGISTICS_TRACKER_TOOL = {
    "name": "CheckLogistics",
    "description": (
        "查询订单的物流状态和配送轨迹。"
        "输入: 订单号(order_id)"
        "返回: 当前状态(运输中/已签收/异常)、签收时间、签收人、物流节点时间线"
    ),
    "input_schema": {
        "order_id": "string (required)"
    }
}

# 工具3: 退款政策查询
POLICY_CHECKER_TOOL = {
    "name": "CheckRefundPolicy",
    "description": (
        "根据退款原因和订单类型，查询公司退款政策并返回适用条款。"
        "输入: 退款原因类别 + 订单创建时间"
        "返回: 是否在退款期内、需满足的条件、审批权限级别"
    ),
    "input_schema": {
        "reason_category": "string - '质量问题'/'物流损坏'/'无理由退货'/'未收到货'/'其他'",
        "order_date": "string - 订单创建日期",
        "order_amount": "float - 订单金额"
    }
}

# 工具4: 邮件发送
EMAIL_SENDER_TOOL = {
    "name": "SendEmail",
    "description": (
        "向用户发送邮件。输入: 收件人邮箱、邮件主题、邮件正文(支持HTML)。"
        "发送前会进行内容安全审核。"
    ),
    "input_schema": {
        "to_email": "string",
        "subject": "string",
        "body": "string (HTML supported)"
    }
}

# 工具5: 用户历史查询
USER_HISTORY_TOOL = {
    "name": "LookupUserHistory",
    "description": (
        "查询用户的购买历史、退款记录、客服交互记录。"
        "用于识别是否存在恶意退款模式。"
    ),
    "input_schema": {
        "user_id": "string (required)",
        "history_type": "string - 'purchase'/'refund'/'support_tickets'"
    }
}
```

### 7.3 平衡公司利益与用户友好的提示词设计

```python
CUSTOMER_SERVICE_PROMPT = """
你是一位专业、富有同理心的电商客服智能助手。你在处理每一次退款申请时，
需要同时兼顾两个目标：
1. 遵守公司退款政策，保护公司利益
2. 理解用户处境，提供友好、体贴的服务体验

## 核心原则
- **政策是底线，不能突破**: 不符合政策的请求必须委婉拒绝，但始终提供替代方案
- **同理心是桥梁**: 先共情，再解释。说"不"之前先表达理解
- **透明决策**: 向用户解释批准/拒绝的具体原因，引用明确的政策条款
- **主动帮助**: 即使需要拒绝退款，也主动提供其他补偿（优惠券、换货、维修等）

## 决策流程
1. 完整收集信息（订单、物流、用户历史、适用政策）
2. 按政策条款逐条核对
3. 计算置信度: 完全匹配政策=高置信度, 灰色地带=低置信度
4. 低置信度时启动审慎模式（自我反思 + 建议转人工）

## 回复风格要求
- 开头: 先感谢/共情（如"感谢您的耐心等待，我理解您对这次购物体验的担忧"）
- 中间: 清晰说明调查结果和政策依据
- 结尾: 给出明确的下一步行动 + 祝福语

## 严格禁止
- 对用户的不礼貌或指责性语言
- 在未核实全部信息前做出承诺
- 透露公司内部敏感信息（如利润数据、其他用户的投诉）
"""
```

### 7.4 风险分析与缓解策略


| 风险类别      | 具体风险                        | 技术缓解措施                                                    |
| --------- | --------------------------- | --------------------------------------------------------- |
| **决策错误**  | AI 误批或误拒退款，造成经济损失或用户投诉      | 1. 低置信度案例强制转人工 2. A/B 测试逐步放量 3. 每日人工抽检决策样本                |
| **提示注入**  | 用户在退款理由中注入恶意提示词             | 1. 用户输入与系统提示词严格隔离（不同 message role） 2. 输入内容过滤 3. 输出独立审核    |
| **公平性问题** | AI 对不同用户群体存在偏见（如 VIP 更容易获批） | 1. 定期审计决策分布 2. 决策依据必须可解释 3. 政策一致性校验                       |
| **隐私泄露**  | AI 在邮件中错误暴露其他用户的订单信息        | 1. 变量注入而非拼接敏感数据 2. 发送前内容审计 3. 用户数据访问权限最小化                 |
| **法律合规**  | 退款决策违反消费者保护法                | 1. 政策审核必须有法律顾问参与 2. 法规变更时同步更新政策库 3. 保留完整决策审计日志            |
| **模型幻觉**  | AI 编造不存在的政策条款               | 1. 所有政策引用必须来自 PolicyChecker 工具返回的实际条款 2. 禁止 AI 自行"补充"政策内容 |
| **规模化挑战** | 并发请求激增导致超时                  | 1. 排队机制 + 优雅降级(超时转人工) 2. 缓存常用政策查询结果 3. 弹性扩容               |
| **社交工程**  | 恶意用户系统性测试 AI 边界寻找漏洞         | 1. 异常行为检测(同用户多次测试性请求) 2. 高危操作速率限制 3. 安全团队定期红队测试           |


---

## 总结：范式选择决策树

```
需要调用外部工具/获取实时信息？
├─ 是 ──→ 任务可以被清晰分解为独立步骤？
│         ├─ 是, 且外部依赖少 ──→ Plan-and-Solve
│         ├─ 是, 但依赖高度不确定 ──→ ReAct
│         └─ 否 ──→ ReAct
│
└─ 否 ──→ 任务的核心挑战是"质量优化"而非"信息获取"？
          ├─ 是 ──→ Reflection
          └─ 否 ──→ 任务复杂度决定了选 Plan-and-Solve(复杂) 还是直接对话(简单)
```

最终，生产级系统往往需要组合范式。单一范式的简洁性适合原型，但真实场景的复杂性要求"取各家之长"。