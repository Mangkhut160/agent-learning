# 智能体范式与 Function Calling 学习笔记

> 学习路径：三种经典范式 → 工具系统 → 提示词工程 → Function Calling → Schema

---

## 第一章：三种智能体范式

### 1.1 ReAct（Reasoning + Acting）

**核心思想**："边想边做"，思考与行动交替执行。

```
Thought₁ → Action₁ → Observation₁ → Thought₂ → Action₂ → ... → Answer
```

每一步只规划下一步，行动的结果（Observation）立刻注入下一轮思考，实时修正方向。

**适用场景**：需要调用外部工具获取实时信息的任务（搜索、查询数据库、控制设备）。

**你的项目**：`react_agent_project/`。核心文件 `src/agent.py`，通过正则解析 LLM 输出的 `Thought:` 和 `Action:` 标记来驱动循环。

---

### 1.2 Plan-and-Solve（先规划后执行）

**核心思想**："先谋后动"，规划与执行彻底分离。

```
Question → Plan = [Step₁, Step₂, ..., Stepₙ]
         → Execute(Step₁) → Execute(Step₂) → ... → Execute(Stepₙ) → Answer
```

- **规划阶段**：LLM 一次性生成完整的静态计划（Python list 格式）
- **执行阶段**：严格按计划顺序执行，每步结果累积到 history，作为下一步的上下文

**适用场景**：结构性强、可被清晰分解的复杂任务（多步数学题、代码架构设计）。

**你的项目**：`plan_solve_agent_project/`。`planner.py` 用 `ast.literal_eval` 解析 LLM 输出的计划列表；`executor.py` 循环执行每步并维护 history 字符串。

**局限性**：计划是静态的，执行中某步失败时无法自动调整。改进方案是在 `executor.py` 中加入"重规划"逻辑——当某步结果不符合预期时，基于已完成步骤重新生成后续计划。

---

### 1.3 Reflection（执行-反思-优化）

**核心思想**："先做出来，再批判改进"，通过自我批判驱动迭代优化。

```
Execute₀ → Reflect₁ → Execute₁ → Reflect₂ → Execute₂ → ... → 最优方案
```

三个角色由同一 LLM 通过不同提示词扮演：
- **程序员**（INITIAL_PROMPT）：生成初版代码
- **评审专家**（REFLECT_PROMPT）：批判性分析，找出瓶颈
- **程序员**（REFINE_PROMPT）：根据反馈优化

**终止条件**：评审返回"无需改进"或达到最大迭代次数。

**适用场景**：有明确质量标准的生成任务（代码优化、论文润色、方案改进）。

**你的项目**：`reflection_agent_project/`。`memory.py` 存储每次执行和反思的完整轨迹；`reflection_agent.py` 协调三轮角色切换。

**关键设计**：Memory 模块是迭代的前提——没有记忆就无法知道"上一版长什么样、上次反馈说了什么"。`get_last_execution()` 倒序遍历 records 获取最新代码；`get_trajectory()` 将完整轨迹序列化为文本供提示词使用。

---

### 1.4 三种范式对比

| 维度 | ReAct | Plan-and-Solve | Reflection |
|------|-------|----------------|------------|
| 思考粒度 | 下一步 | 整个计划 | 整个方案 |
| 行动粒度 | 单个工具调用 | 单个子步骤 | 完整任务 |
| 信息增量 | 外部观察 | 前序步骤结果 | 批判性反馈 |
| 灵活性 | 高（可随时调整） | 低（计划固定） | 中（迭代改进） |
| 核心依赖 | 外部工具 | 任务可分解性 | 自批判能力 |
| 调用次数 | 不确定（直到 Finish） | N+1 次 | 3-6 次（取决于收敛速度） |

**范式选择决策树：**

```
需要调用外部工具/获取实时信息？
├─ 是 → ReAct（或 Plan+ReAct 混合）
└─ 否 → 任务是"质量优化"还是"一次性求解"？
        ├─ 质量优化 → Reflection
        └─ 一次性求解且步骤多 → Plan-and-Solve
```

**混合范式实例**（P+R³ 架构）：
```
Planner → Executor(每步用ReAct执行) → Reflector(评估结果) → Refiner(优化方案)
```
适用场景：需要工具调用、步骤间有依赖、质量要求高的复杂任务（如旅行预订、客服系统）。

---

## 第二章：工具系统

### 2.1 正则解析的脆弱性

当前 ReAct 项目用正则提取 `Thought:` 和 `Action:`：

```python
# agent.py 中的解析逻辑
match = re.match(r"(\w+)\[(.*)\]", action_text, re.DOTALL)
```

失败场景：

| 场景 | 示例 | 失败原因 |
|------|------|----------|
| 中文冒号 | `思考：xxx` | `Thought:` 正则不匹配 |
| 嵌套括号 | `Search[华为P70 [Pro版]]` | `(.*)\]` 在第一个 `]` 截断 |
| 省略标记 | `搜索华为手机` | 完全无法解析 |
| 模型发散 | 输出一大段自然语言 | 正则找不到目标模式 |

### 2.2 升级为 Function Calling

核心变化：**不再让模型输出自由文本再解析，而是通过 API 层面约束模型输出结构化 JSON。**

改造要点：

1. 每个工具注册时多传一个 `parameters` schema：

```python
tool_executor.registerTool(
    name="Calculator",
    description="执行数学计算",
    func=calculator,
    parameters={                    # ← 新增
        "type": "object",
        "properties": {
            "expression": {
                "type": "string",
                "description": "数学表达式"
            }
        },
        "required": ["expression"]
    }
)
```

2. 调用 API 时传 `tools` 参数，模型返回 `tool_calls` 而非自由文本：

```python
response = client.chat.completions.create(
    model=model_name,
    messages=messages,
    tools=tools_schema,      # ← 告诉模型有哪些工具
    tool_choice="auto"       # ← 让模型决定是否调用
)

msg = response.choices[0].message
if msg.tool_calls:
    # 直接取 JSON，不需要正则
    args = json.loads(msg.tool_calls[0].function.arguments)
```

### 2.3 大规模工具管理（50-100+ 工具）

当工具数量激增时，全量注入提示词会导致上下文溢出和注意力稀释。解决方案：

**分层管理架构：**
```
Layer 1: 工具注册中心（元数据 + 语义向量索引 + 分类标签）
Layer 2: 动态检索（意图分类 → 语义搜索 → 返回 Top-5 候选）
Layer 3: 工具选择（将候选注入提示词 → LLM 精确选择）
```

**核心思路**：不是让模型从 100 个里选，而是先检索出 3-5 个最相关的，让模型从少量候选中选。

---

## 第三章：Function Calling 与 Schema

### 3.1 Function Calling 能保证结构化输出的原因

**三层机制共同作用：**

1. **训练数据对齐**：模型在训练阶段被喂了大量"给定 schema → 输出符合 schema 的 JSON"的样本，已经学会了遵循 schema。

2. **Grammar-Constrained Decoding**：推理时，API 将 schema 编译成有限状态机，**在生成每个 token 时限制可选范围**。结构骨架（括号、引号、字段名）被锁死，模型只能在值的位置自由发挥。

   ```
   Schema → 编译 → 状态机:
   START → { → "query" → : → " → <生成字符串> → " → } → END
                                                  ↑
                                        只能走这条路
   ```

3. **API 层面校验**：即使模型输出有偏差，API 服务端会拒绝不符合 schema 的返回。

**本质差异**：自然语言提示词是"建议"，schema 是"强制约束"。前者模型可能不遵守，后者模型在物理上无法违反。

---

### 3.2 Schema 规范详解

Schema 遵循 **JSON Schema 子集**，结构如下：

```
顶层: type, function.name, function.description, function.parameters
  └─ parameters: type: "object", properties, required
       └─ 每个 property: type, description, enum/format/pattern/minimum...
```

#### 基本类型速查

| type | 说明 | 独有约束 |
|------|------|----------|
| `"string"` | 字符串 | `enum`, `format`(email/date/uri), `pattern`, `maxLength` |
| `"number"` | 数字（含小数） | `minimum`, `maximum`, `enum` |
| `"integer"` | 整数 | 同上 |
| `"boolean"` | 布尔值 | 无额外约束 |
| `"array"` | 数组 | `items`(元素类型, **必填**), `minItems`, `maxItems` |
| `"object"` | 嵌套对象 | `properties`, `required`（可无限嵌套） |

#### 常见错误

1. **`required` 写在 property 内部** → 正确：`required` 是 `parameters` 层级的数组 `["param1", "param2"]`
2. **`array` 忘了写 `items`** → 模型不知道数组元素是什么类型
3. **`description` 太模糊** → 模型填参数时缺少指引，导致乱填

#### 完整示例（预订会议室）

```json
{
    "type": "function",
    "function": {
        "name": "BookMeetingRoom",
        "description": "预订一间会议室",
        "parameters": {
            "type": "object",
            "properties": {
                "room_type": {
                    "type": "string",
                    "enum": ["small", "medium", "large"],
                    "description": "会议室大小"
                },
                "date": {
                    "type": "string",
                    "format": "date",
                    "description": "预订日期"
                },
                "start_time": {
                    "type": "string",
                    "pattern": "^([01]\\d|2[0-3]):[0-5]\\d$",
                    "description": "开始时间，格式 HH:MM"
                },
                "duration_hours": {
                    "type": "integer",
                    "minimum": 1,
                    "maximum": 8,
                    "description": "时长（小时）"
                },
                "attendees": {
                    "type": "array",
                    "items": {"type": "string", "format": "email"},
                    "minItems": 1,
                    "description": "参会人邮箱"
                },
                "equipment": {
                    "type": "object",
                    "description": "需要的设备",
                    "properties": {
                        "projector": {"type": "boolean"},
                        "video_conference": {"type": "boolean"}
                    }
                },
                "notes": {
                    "type": "string",
                    "maxLength": 500,
                    "description": "备注"
                }
            },
            "required": ["room_type", "date", "start_time", "duration_hours"]
        }
    }
}
```

---

## 第四章：提示词工程要点

### 4.1 提示词结构服务于范式逻辑

| | ReAct 提示词 | Plan-and-Solve 提示词 | Reflection 提示词 |
|---|---|---|---|
| **角色** | 通用助手 | 顶级规划专家 | 程序员 / 评审专家（切换） |
| **输出格式** | 自由文本 | Python 列表（可解析） | 代码 / 反馈（切换） |
| **状态变量** | `{history}` 每轮变化 | 无 history | `{last_code}` + `{feedback}` |
| **设计目标** | 在不确定性中保持方向感 | 一次性产出可执行的完整蓝图 | 驱动方案质量阶梯式提升 |

### 4.2 角色设定的影响力

同一段代码，不同角色设定会产生完全不同的反馈：

- **"极其严格的性能专家"** → 关注时间复杂度、内存开销
- **"开源项目维护者"** → 关注可读性、命名规范、注释
- **"安全审计专家"** → 关注输入验证、边界条件、注入风险

**原则**：角色 + 具体场景约束 > 泛化角色。选角色时要精确到"评审什么维度"。

### 4.3 Few-shot 的价值

在提示词中加入 3 个格式示例，可将模型的格式遵循率从 70-85% 提升到 90-98%。代价是额外消耗约 500 tokens。对于正则解析方案，few-shot 几乎必加；对于 Function Calling 方案，格式由 schema 保证，few-shot 的价值转向"教会模型何时选哪个工具"。

---

## 第五章：项目文件对照表

你的 `agent_learn` 目录下现在有三个完整项目：

```
agent_learn/
├── react_agent_project/          # ReAct 范式 + 正则解析
│   ├── src/agent.py              #   Thought-Action 循环 + 正则解析
│   ├── src/tool_executor.py      #   工具注册与调用（3 参数）
│   └── src/tools.py              #   Search + Calculator
│
├── plan_solve_agent_project/     # Plan-and-Solve 范式
│   ├── src/planner.py            #   规划器（ast.literal_eval 解析计划）
│   ├── src/executor.py           #   执行器（循环执行 + history 累积）
│   └── src/plan_solve_agent.py   #   协调器
│
├── reflection_agent_project/     # Reflection 范式
│   ├── src/memory.py             #   短期记忆模块
│   ├── src/reflection_agent.py   #   执行-反思-优化循环
│   └── src/llm_client.py         #   含 <think> 标签清洗
│
└── 智能体范式深度分析与实践.md    # 综合分析文档（课后题详解）
```

三项目共用同一套 LLM 客户端模式（`HelloAgentsLLM`），只是各范式对提示词、状态管理、循环逻辑的处理完全不同。

---

## 附录：运行命令

```bash
# ReAct
cd react_agent_project && pip install -r requirements.txt && py main.py

# Plan-and-Solve
cd plan_solve_agent_project && pip install -r requirements.txt && py main.py

# Reflection
cd reflection_agent_project && pip install -r requirements.txt && py main.py

# 交互模式（三个项目都支持）
py main.py --interactive

# 解决中文编码问题
PYTHONIOENCODING=utf-8 py main.py
```
